node_modules
.env
.DS_Store
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Optional: Ignore data if you want to keep real data private, 
# but for now we kept it as it was structure.
# data/*.json
